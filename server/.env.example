PORT=4000
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/bookleaf_portal?retryWrites=true&w=majority
JWT_SECRET=replace_me_with_long_random_string

# We'll use later (tickets AI). Keep server-side only.
GROQ_API_KEY=replace_me
GROQ_MODEL=llama-3.3-70b-versatile